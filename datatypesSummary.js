// Based on how the data is been stored in the memory  and how the data is been accessed from the memory.
// The datatype is been categorized as , namely :
// 1. Primitive datatype (Call-by-value) : 
//      7 types - String, Number, Boolean, null, undefined, Symbol, BigInt.

// 2. Non-primitive datatype (Call-by-reference) :
//      Array, objects and Functions

// Declaration of Array, objects and Functions

let myArray = ["Chetan", 19, 2003 ]

const myObj = {
    name : Chetan,
    age : 22,
}

const myFunction = function () {
    console.log("Hello world");
}